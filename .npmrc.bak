# 📦 Install performance and reliability
network-timeout=600000
fetch-retries=3
fetch-retry-mintimeout=5000
fetch-retry-maxtimeout=60000
loglevel=error
fund=false
audit=false

# 🚫 Prevent browser binary downloads (Puppeteer, Playwright, Chromium, etc.)
PUPPETEER_SKIP_DOWNLOAD=true
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
SKIP_PUPPETEER_DOWNLOAD=true
PUPPETEER_NO_DOWNLOAD=true
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=true

# 🚫 Disable browser bin usage
CHROME_BIN=/bin/false
CHROMIUM_BIN=/bin/false
PUPPETEER_EXECUTABLE_PATH=/bin/false

# 🛑 Disable native dependency issues (Rollup, Puppeteer, etc.)
legacy-peer-deps=true
